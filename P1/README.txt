In this program, we read arguments and set correspondig flags. 
If arguments are not expected, the program outputs a bad argument message and gently terminate the program.
We read all the information we need from all the possible processes that will be displayed from /proc and store them
then based on users' argument, we output information that is required by users